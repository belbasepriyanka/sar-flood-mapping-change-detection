# Data provenance

`sample_event_pixels.csv` is a small extract generated from a **synthetic flood event** by `src/pipeline.py`. It contains no real disaster observations. The runnable script regenerates the full synthetic event.

For real deployment, replace it with event-specific Sentinel-1 or aerial imagery plus validated flood-reference data, DEM/slope, permanent-water layers, population, roads and critical infrastructure.
